A vocabulary trainer developed for querying german-japanese
vocabulary.

It now uses gtk3 and sqlite.
To run it, you need python 3,
python-gobject, gtk and sqlite.

Any configuration (especially paths, fonts and strings)
can be modified in config/config.py ...

